/**
 * Public API for the AI feature boundary (chat widget + behavior tracking).
 * Import from this barrel only — do not deep-import files under ai-engine/.
 */
export { ChatWidget } from './chat/chat-widget';
export { TrackBehaviorDirective } from './directives/track-behavior.directive';
